# ORCA Output Parser - Python Dependencies
# Install: pip install -r requirements.txt

# Web Framework (for UI)
Flask==3.0.0
Werkzeug==3.0.1

# Testing Framework
pytest==7.4.3
pytest-cov==4.1.0

# Optional: For enhanced JSON handling
# (stdlib json is sufficient, but this adds features)
# orjson==3.9.10

# Note: All parsing uses Python standard library only
# Core dependencies: re, json, dataclasses (stdlib)
# Python 3.10+ required for proper dataclass support
